#!/bin/sh
set -eu

ROOT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$ROOT_DIR"

if [ -f "$ROOT_DIR/.venv/bin/activate" ]; then
  . "$ROOT_DIR/.venv/bin/activate"
fi

exec python3 "$ROOT_DIR/health_research_monitor.py" "$@"
